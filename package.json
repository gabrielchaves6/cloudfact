{
  "name": "cloudfact",
  "version": "0.2.0",
  "description": "MCP + CLI para publicar páginas estáticas da VM na Cloudflare (túnel rápido ou Pages) com um comando.",
  "type": "module",
  "bin": {
    "cloudfact": "dist/cloudfact.js"
  },
  "scripts": {
    "build": "node build.js",
    "start": "node dist/server.js",
    "test": "npm run build && node test/smoke.js",
    "prepare": "node build.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "files": [
    "dist",
    "src",
    "bin",
    "skills",
    ".claude-plugin",
    
    "install.sh",
    "README.md",
    "LICENSE"
  ]
}
